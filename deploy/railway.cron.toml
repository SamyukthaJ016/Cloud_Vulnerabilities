[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = [
  "backend/**",
  "config/**",
  "db/init/**",
  "deploy/**",
  "Dockerfile",
  "requirements.txt",
]

[deploy]
startCommand = "sh deploy/run-due-schedules.sh"
restartPolicyType = "NEVER"
cronSchedule = "*/5 * * * *"
