services:
  postgres:
    image: postgres:15-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-scanner_db}
      POSTGRES_USER: ${POSTGRES_USER:-scanner_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ../../db/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-scanner_user} -d ${POSTGRES_DB:-scanner_db}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ../..
      dockerfile: Dockerfile
      target: production
    restart: unless-stopped
    environment:
      DATABASE_URL: ${DATABASE_URL}
      QUEUE_BACKEND: postgres
      SCAN_JOB_INLINE_WORKER: "false"
      SANDBOX_LAB_INLINE_WORKER: "false"
      SANDBOX_LABS_ENABLED: ${SANDBOX_LABS_ENABLED:-true}
      SANDBOX_IAC_DEPLOY: ${SANDBOX_IAC_DEPLOY:-true}
      SANDBOX_AWS_DEPLOY: ${SANDBOX_AWS_DEPLOY:-false}
      SANDBOX_GCP_DEPLOY: ${SANDBOX_GCP_DEPLOY:-false}
      SANDBOX_KUBERNETES_DEPLOY: ${SANDBOX_KUBERNETES_DEPLOY:-false}
      SANDBOX_LAB_BASE_DIR: ${SANDBOX_LAB_BASE_DIR:-/app/sandbox-labs}
      WORKER_AUTH_REQUIRED: "true"
      WORKER_TOKEN: ${WORKER_TOKEN}
      WORKER_HEARTBEAT_STALE_SECONDS: ${WORKER_HEARTBEAT_STALE_SECONDS:-45}
      SCAN_JOB_TIMEOUT_MINUTES: ${SCAN_JOB_TIMEOUT_MINUTES:-20}
      OBJECT_STORAGE_PROVIDER: ${OBJECT_STORAGE_PROVIDER:-digitalocean-spaces}
      OBJECT_STORAGE_ENDPOINT_URL: ${OBJECT_STORAGE_ENDPOINT_URL}
      OBJECT_STORAGE_REGION: ${OBJECT_STORAGE_REGION:-blr1}
      OBJECT_STORAGE_BUCKET: ${OBJECT_STORAGE_BUCKET}
      OBJECT_STORAGE_ACCESS_KEY_ID: ${OBJECT_STORAGE_ACCESS_KEY_ID}
      OBJECT_STORAGE_SECRET_ACCESS_KEY: ${OBJECT_STORAGE_SECRET_ACCESS_KEY}
      OBJECT_STORAGE_PREFIX: ${OBJECT_STORAGE_PREFIX:-cloudguard/evidence}
      AI_BASE_URL: ${AI_BASE_URL}
      AI_MODEL: ${AI_MODEL}
      AI_API_KEY: ${AI_API_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_AGENT_MODEL: ${OPENAI_AGENT_MODEL:-}
      SECRET_KEY: ${SECRET_KEY}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY}
      ALLOWED_HOSTS: ${ALLOWED_HOSTS:-*}
      CORS_ORIGINS: ${CORS_ORIGINS:-https://${DOMAIN:-localhost}}
      APP_ENV: production
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      PYTHONPATH: /app
    depends_on:
      postgres:
        condition: service_healthy
    expose:
      - "8000"
    ports:
      - "${BACKEND_BIND_ADDRESS:-127.0.0.1}:${BACKEND_PORT:-8000}:8000"

  scan-worker:
    build:
      context: ../..
      dockerfile: Dockerfile
      target: production
    restart: always
    command: ["python", "backend/scripts/scan_job_worker.py"]
    environment:
      CLOUDGUARD_BASE_URL: http://backend:8000
      WORKER_TOKEN: ${WORKER_TOKEN}
      SCAN_WORKER_ID: ${SCAN_WORKER_ID:-do-scan-worker-1}
      SCAN_WORKER_POLL_INTERVAL: ${SCAN_WORKER_POLL_INTERVAL:-10}
      WORKER_HEARTBEAT_INTERVAL: ${WORKER_HEARTBEAT_INTERVAL:-15}
      SCAN_WORKER_REQUEST_TIMEOUT: ${SCAN_WORKER_REQUEST_TIMEOUT:-900}
      PYTHONPATH: /app
    depends_on:
      - backend

  scheduler-worker:
    build:
      context: ../..
      dockerfile: Dockerfile
      target: production
    restart: always
    command: ["python", "-m", "backend.scheduler_worker"]
    environment:
      DATABASE_URL: ${DATABASE_URL}
      WORKER_TOKEN: ${WORKER_TOKEN}
      SCHEDULER_WORKER_ID: ${SCHEDULER_WORKER_ID:-do-scheduler-worker-1}
      WORKER_HEARTBEAT_INTERVAL: ${WORKER_HEARTBEAT_INTERVAL:-15}
      AI_BASE_URL: ${AI_BASE_URL}
      AI_MODEL: ${AI_MODEL}
      AI_API_KEY: ${AI_API_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_AGENT_MODEL: ${OPENAI_AGENT_MODEL:-}
      APP_ENV: scheduler
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      PYTHONPATH: /app
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM_EMAIL: ${SMTP_FROM_EMAIL:-}
    depends_on:
      postgres:
        condition: service_healthy
      backend:
        condition: service_started

  sandbox-lab-worker:
    build:
      context: ../..
      dockerfile: Dockerfile
      target: production
    restart: always
    command: ["python", "backend/scripts/sandbox_lab_worker.py"]
    environment:
      DATABASE_URL: ${DATABASE_URL}
      SANDBOX_LABS_ENABLED: ${SANDBOX_LABS_ENABLED:-true}
      SANDBOX_IAC_DEPLOY: ${SANDBOX_IAC_DEPLOY:-true}
      SANDBOX_AWS_DEPLOY: ${SANDBOX_AWS_DEPLOY:-false}
      SANDBOX_GCP_DEPLOY: ${SANDBOX_GCP_DEPLOY:-false}
      SANDBOX_KUBERNETES_DEPLOY: ${SANDBOX_KUBERNETES_DEPLOY:-false}
      SANDBOX_LAB_BASE_DIR: ${SANDBOX_LAB_BASE_DIR:-/app/sandbox-labs}
      SANDBOX_LAB_WORKER_ID: ${SANDBOX_LAB_WORKER_ID:-do-sandbox-lab-worker-1}
      SANDBOX_LAB_WORKER_POLL_INTERVAL: ${SANDBOX_LAB_WORKER_POLL_INTERVAL:-10}
      WORKER_HEARTBEAT_INTERVAL: ${WORKER_HEARTBEAT_INTERVAL:-15}
      OBJECT_STORAGE_PROVIDER: ${OBJECT_STORAGE_PROVIDER:-digitalocean-spaces}
      OBJECT_STORAGE_ENDPOINT_URL: ${OBJECT_STORAGE_ENDPOINT_URL}
      OBJECT_STORAGE_REGION: ${OBJECT_STORAGE_REGION:-blr1}
      OBJECT_STORAGE_BUCKET: ${OBJECT_STORAGE_BUCKET}
      OBJECT_STORAGE_ACCESS_KEY_ID: ${OBJECT_STORAGE_ACCESS_KEY_ID}
      OBJECT_STORAGE_SECRET_ACCESS_KEY: ${OBJECT_STORAGE_SECRET_ACCESS_KEY}
      OBJECT_STORAGE_PREFIX: ${OBJECT_STORAGE_PREFIX:-cloudguard/evidence}
      AI_BASE_URL: ${AI_BASE_URL}
      AI_MODEL: ${AI_MODEL}
      AI_API_KEY: ${AI_API_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_AGENT_MODEL: ${OPENAI_AGENT_MODEL:-}
      APP_ENV: sandbox-worker
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      PYTHONPATH: /app
    volumes:
      - sandbox_lab_data:/app/sandbox-labs
    depends_on:
      postgres:
        condition: service_healthy
      backend:
        condition: service_started

volumes:
  postgres_data:
  sandbox_lab_data:
