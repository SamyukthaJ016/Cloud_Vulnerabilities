import { ComingSoon } from '@/components/ComingSoon';

export function WebhooksPage() {
  return (
    <ComingSoon
      title="Webhooks"
      description="Register URLs to receive signed events on scans, findings, and billing changes."
      flowNodes="Q1 (external)"
    />
  );
}
