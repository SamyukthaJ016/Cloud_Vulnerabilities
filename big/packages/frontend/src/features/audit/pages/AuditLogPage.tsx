import { ComingSoon } from '@/components/ComingSoon';

export function AuditLogPage() {
  return (
    <ComingSoon
      title="Audit log"
      description="Every significant action by users and the system, filterable by actor and resource."
      flowNodes="X5, TS6"
    />
  );
}
