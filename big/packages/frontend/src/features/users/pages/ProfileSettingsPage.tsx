import { ComingSoon } from '@/components/ComingSoon';

export function ProfileSettingsPage() {
  return (
    <ComingSoon
      title="Profile"
      description="Update your name and avatar. Email is managed by your SSO provider."
      flowNodes="(settings)"
    />
  );
}
