import { ComingSoon } from '@/components/ComingSoon';

export function OrgSettingsPage() {
  return (
    <ComingSoon
      title="Organization"
      description="Edit organization profile, slug, and domain."
      flowNodes="X1"
    />
  );
}
