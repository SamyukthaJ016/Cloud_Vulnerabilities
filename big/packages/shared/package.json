{
  "name": "@cloudguard/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Wire-contract types shared between CloudGuard backend and frontend",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "echo 'shared package has no lint step (type-only)'",
    "test": "echo 'shared package has no tests'"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
